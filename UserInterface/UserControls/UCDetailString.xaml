<UserControl x:Class="UserInterface.UserControls.UCDetailString"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" TextAlignment="Left" Text="{Binding Description}" FontSize="{StaticResource SmallFontSize}"/>
        <TextBlock Grid.Column="1" TextAlignment="Left" Text="{Binding Value}" FontSize="{StaticResource SmallFontSize}"/>
    </Grid>
</UserControl>
